{
  "name": "post_office_project",
  "version": "1.0.0",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./server/server.js",
    "client": "cd client && npm start"
  },
  "description": "",
  "dependencies": {
    "express": "^4.21.1",
    "if-env": "^1.0.4",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.11.3"
  }
}
